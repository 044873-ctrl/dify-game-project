name: Update Game from Dify

on:
  workflow_dispatch:
  repository_dispatch:
    types: [update_game]

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug payload (safe)
        run: |
          echo "Payload exists:"
          echo "${{ github.event.client_payload.code_base64 }}" | head -c 120 || true
          echo ""

      - name: Write index.html
        run: |
          PAYLOAD="${{ github.event.client_payload.code_base64 }}"

          # 1. 空チェック
          if [ -z "$PAYLOAD" ]; then
            echo "<html><body><h1>No payload received</h1></body></ht
